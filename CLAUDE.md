@AGENTS.md

## Claude Code

`AGENTS.md` is this repo's canonical instruction file (vendor-neutral). The
`@AGENTS.md` import line above makes Claude Code load the same instructions that
other coding agents read, so there is a single source of truth. Any
Claude-Code-specific notes go here; everything else lives in `AGENTS.md`.