// Package integration contains tooling-integration tests for the Eidos
// generator (e.g. the GoReleaser snapshot build test in
// goreleaser_snapshot_test.go). The OpenAPI specs exercised by the golden
// regression tests live under test/specs; shared golden snapshots and live
// end-to-end fixtures live under testfixtures/ (not test/fixtures, which is
// empty).
package integration
